package tw.com.slsinfo.panel.wrapper.main;

import org.apache.wicket.markup.html.panel.Panel;
import org.apache.wicket.model.IModel;

public class PartnerPanel extends Panel {
    public PartnerPanel(String id) {
        super(id);
    }

    public PartnerPanel(String id, IModel<?> model) {
        super(id, model);
    }
}
